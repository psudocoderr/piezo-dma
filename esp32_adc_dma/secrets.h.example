#pragma once

// Copy this file to secrets.h (which should already be in your .gitignore)
// and fill in real values. Never commit secrets.h.

#define WIFI_SSID     "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Host receiving the binary UDP stream.
#define PIEZO_UDP_HOST "192.168.1.100"
#define PIEZO_UDP_PORT 5005

// TCP server that will receive the piezo sample stream
#define TCP_SERVER_HOST "192.168.1.100"
#define TCP_SERVER_PORT 5000
